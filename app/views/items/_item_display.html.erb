<p class="todo-item">
  <%= item.description %>
  <span class="item-controls">
    <%= link_to "Edit", edit_item_path(item) %> | <%= link_to "Delete", item_path(item), :method => :delete %>
  </span>
</p>
